---
jupyter:
  jupytext:
    text_representation:
      extension: .md
      format_name: markdown
      format_version: '1.3'
      jupytext_version: 1.18.1
  kernelspec:
    display_name: base
    language: python
    name: python3
---

<!-- #region -->
# 0.1 Introduction to Types

Python (as most high-level programming languages) is based of a simple yet powerful philosophy: **Everything is an Object**.

Imagine you are organizing your home. Every object has:
- **A Type** (Book, Chair, Cup)
- **Properties** (Color, Size, Material)
- **Capabilities** (What can you do with it?)


Same principle applies in Python: Every object has a **type** with specific **properties** and **capabilities**.
<!-- #endregion -->

## Why do types matter?

Types are like categories in real life. They help us:

1. **Safety**: We know what to expect
2. **Functionality**: Different types can do different things
3. **Abstraction**: We think in concepts, not in details

**Example from real life**:
- With a *hammer* you can hammer
- With a *screwdriver* you can screw
- With a *number* you can calculate
- With a *text* you can write


## For basic types in Python, we have:
- **Integers (`int`)**: Whole numbers, e.g., `1`, `42`, `-7`
- **Floating-point numbers (`float`)**: Decimal numbers, e.g., `3.14`, `-0.001`
- **Strings (`str`)**: Text, e.g., `"Hello, World!"`, `'Python is fun'`
- **Booleans (`bool`)**: True/False values, e.g., `True`, `False`




> ### **Why this matters for CADET-Process:**
- **Integers**: Number of components, discretization points, time steps
- **Floats**: Concentrations, flow rates, temperatures, column dimensions  
- **Strings**: Component names, unit operation types, file paths
- **Booleans**: Simulation flags, convergence status, configuration options

```python
# 1. Integers (whole numbers)
age = 25
year = 2024
temp = -5

print(f"Age: {age}")
print(f"Year: {year}")
print(f"Temperature: {temp}")
```

```python
# 2. Floats (decimal numbers)
pi = 3.14159
weight = 75.5
percentage = 0.85

print(f"Pi: {pi}")
print(f"Weight: {weight} kg")
print(f"Percentage: {percentage * 100}%")

```

```python
# 3. String (text)
name = "Anna"
city = 'Berlin'
message = """this is a
multiline text"""

print(f"Name: {name}")
print(f"City: {city}")
print(f"Message: {message}")

```

```python
# 4. Boolean (True/False)
is_student = True
has_license = False
is_adult = age >= 18  # This is a calculation!

print(f"Is student: {is_student}")
print(f"Has license: {has_license}")
print(f"Is adult: {is_adult}")
```

## Comparison Operators - Building Conditions

**Comparison operators** create `bool` values by comparing data:

**All Types:**
- `==` - equal to
- `!=` - not equal to

**Numbers (int, float):**
- `<` - less than
- `>` - greater than  
- `<=` - less than or equal
- `>=` - greater than or equal

**Strings:**
- Alphabetical comparison works with `<`, `>`, etc.
- Case sensitive: `"A" < "a"` is `True`

### **CADET-Process Connection:**
In CADET-Process, you'll frequently use comparisons to:
- Check if concentrations are above/below thresholds: `concentration > 0.5`
- Compare simulation results: `result1.solution_times == result2.solution_times`
- Validate parameters: `flow_rate >= 0` (flow rates can't be negative!)
- String comparisons for component names: `component_name == "Protein_A"`

```python
# Comparison operators with all basic types

# Numbers (int and float)
num1 = 10
num2 = 5.5
print("Number Comparisons:")
print(f"{num1} == {num2}: {num1 == num2}")
print(f"{num1} != {num2}: {num1 != num2}")
print(f"{num1} > {num2}: {num1 > num2}")
print(f"{num1} <= {num2}: {num1 <= num2}")

# Strings
str1 = "apple"
str2 = "banana"
print(f"\nString Comparisons:")
print(f"'{str1}' == '{str2}': {str1 == str2}")
print(f"'{str1}' != '{str2}': {str1 != str2}")
print(f"'{str1}' < '{str2}': {str1 < str2}")  # Alphabetical order
print(f"'{str1}' > '{str2}': {str1 > str2}")

# Case sensitivity
str3 = "Apple"
print(f"'{str1}' == '{str3}': {str1 == str3}")  # Case matters!
print(f"'{str1}' < '{str3}': {str1 < str3}")    # Capital letters come first

# Boolean comparisons
bool1 = True
bool2 = False
print(f"\nBoolean Comparisons:")
print(f"{bool1} == {bool2}: {bool1 == bool2}")
print(f"{bool1} != {bool2}: {bool1 != bool2}")

```

> ### **Question**: What happens when we compare different types?

```python
# print(f"{num1} == '{str1}': {num1 == str1}")
# print(f"{num1} > '{str1}': {num1 > str1}")
```

## Check types 

Sometimes we want to know what type a object has:
We have the built-in function 
- `type()` that returns the type of a given object.
- `isinstance()` that checks if an object is an instance of a specific class or type.

```python
# type() - function that returns the type of a object

theAnswer = 42
pi = 3.14
greeting = "Hello"
is_active = True

print(f"{theAnswer} has type: {type(theAnswer).__name__}")
print(f"{pi} has type: {type(pi).__name__}")
print(f"{greeting} has type: {type(greeting).__name__}")
print(f"{is_active} has type: {type(is_active).__name__}")

# __name__ to get the name of the type as a string, we will understand this later in 0.7 - Classes
```

```python
# isinstance() - Question: "Is this a specific type?"
theAnswer = 42
text = "Hello"

print(f"Is {theAnswer} a number (int) ? {isinstance(theAnswer, int)}")
print(f"Is {text} a number (int) ? {isinstance(text, int)}")
print(f"Is {text} a text (str) ? {isinstance(text, str)}")

# We also can check for multiple types at once
print(f"Is {theAnswer} a number (int or float) ? {isinstance(theAnswer, (int, float))}")
```

<!-- #region -->
> ### **Question**: What type does `int_value` have at the end?

Given the following code snippet:
```python
int_value = 1
int_value = 4.5
```
<!-- #endregion -->

```python
int_value = 1
int_value = 4.5

# print(f"int_value: {int_value}, type: {type(int_value).__name__}")
```

## Typecasting

The type of an object can be changed (if possible) using typecasting functions:
- `int()`: Converts to integer
- `float()`: Converts to floating-point number
- `str()`: Converts to string
- `bool()`: Converts to boolean

```python
# From text to number
text_number = "123"
real_number = int(text_number)
decimal_number = float(text_number)

print(f"Test    : '{text_number}' (Typ: {type(text_number)})")
print(f"As int: {real_number} (Typ: {type(real_number)})")
print(f"As float: {decimal_number} (Typ: {type(decimal_number)})")

# From numbers to text
age = 25
age_text = str(age)
print(f"\nNumber: {age} (Typ: {type(age)})")
print(f"As Text: '{age_text}' (Typ: {type(age_text)})")
```

# Exercise - Types and checking types


## Exercise 1: Type detection

Create different variables and find out what types they have:

```python tags=["solution"]
# Exercise 1: Type detection

# Create different variables
my_age = 28                      # Integer
temperature = 36.5               # Float
my_name = "Alice"               # String
is_student = True               # Boolean
pi_approximation = 3.14159      # Float
favorite_number = 42            # Integer
greeting = 'Hello World'        # String
has_car = False                 # Boolean

# Check their types
print("Variable types:")
print(f"my_age = {my_age}, type: {type(my_age).__name__}")
print(f"temperature = {temperature}, type: {type(temperature).__name__}")
print(f"my_name = '{my_name}', type: {type(my_name).__name__}")
print(f"is_student = {is_student}, type: {type(is_student).__name__}")
print(f"pi_approximation = {pi_approximation}, type: {type(pi_approximation).__name__}")
print(f"favorite_number = {favorite_number}, type: {type(favorite_number).__name__}")
print(f"greeting = '{greeting}', type: {type(greeting).__name__}")
print(f"has_car = {has_car}, type: {type(has_car).__name__}")
```

## Exercise 2: Typecasting

Verwandle die gegebenen Werte in andere Typen:
Change the given values into other types:
- Text "456" in an integer and a float
- Decimal number 3.8 in an integer
- Integer 100 in a text
- Boolean False in a number

```python tags=["solution"]
# Exercise 2: Typecasting

# Text "456" to integer and float
text_number = "456"
as_integer = int(text_number)
as_float = float(text_number)

print("Converting text to numbers:")
print(f"Original: '{text_number}' (type: {type(text_number).__name__})")
print(f"As integer: {as_integer} (type: {type(as_integer).__name__})")
print(f"As float: {as_float} (type: {type(as_float).__name__})")

# Decimal number 3.8 to integer
decimal = 3.8
as_int = int(decimal)
print(f"\nConverting float to int:")
print(f"Original: {decimal} (type: {type(decimal).__name__})")
print(f"As integer: {as_int} (type: {type(as_int).__name__}) - Note: decimal part is lost!")

# Integer 100 to text
number = 100
as_text = str(number)
print(f"\nConverting int to string:")
print(f"Original: {number} (type: {type(number).__name__})")
print(f"As text: '{as_text}' (type: {type(as_text).__name__})")

# Boolean False to number
boolean = False
as_number = int(boolean)
print(f"\nConverting boolean to int:")
print(f"Original: {boolean} (type: {type(boolean).__name__})")
print(f"As number: {as_number} (type: {type(as_number).__name__}) - False = 0, True = 1")
```

## Exercise 3: Calculations with different types

Experiment with operations between different types:
- Add two numbers together (int and float)
- Add a number and a text
- Multiply a text with a number

Check the results and their types using `type()`.


```python tags=["solution"]
# Exercise 3: Calculations with different types

# Add two numbers together (int and float)
int_num = 10
float_num = 3.5
addition_result = int_num + float_num
print("Adding int and float:")
print(f"{int_num} + {float_num} = {addition_result}")
print(f"Result type: {type(addition_result).__name__}")

# Add a number and a text - This will cause an error!
try:
    error_result = int_num + "text"  # This will fail
except TypeError as e:
    print(f"\nTrying to add number and text: ERROR!")
    print(f"Error message: {e}")

# Multiply a text with a number - This works!
text = "Hello"
multiplied_text = text * 3
print(f"\nMultiplying text with number:")
print(f"'{text}' * 3 = '{multiplied_text}'")
print(f"Result type: {type(multiplied_text).__name__}")

# More interesting combinations
print(f"\nMore combinations:")
print(f"True + 5 = {True + 5} (True = 1)")
print(f"False * 10 = {False * 10} (False = 0)")
print(f"3.0 == 3 = {3.0 == 3} (same value, different types)")
print(f"'5' * 2 = '{'5' * 2}' (string repetition)")

# CADET-Process relevance
concentration = 1.5  # mg/mL
volume = 100        # mL
total_mass = concentration * volume  # This works: float * int
print(f"\n CADET example:")
print(f"Concentration: {concentration} mg/mL * Volume: {volume} mL = Total mass: {total_mass} mg")
print(f"Result type: {type(total_mass).__name__}")
```

<!-- #region -->
## Reflektion

Answer the following questions for yourself:

1. What is the most important difference between `int` and `float`?
2. Why does `"5" + 3` give an error, but `"5" * 3` works?
3. When would you use `type()` and when `isinstance()`?
4. What does "Everything is an object" mean to you now?


Write down your answers and discuss them with a partner or in a group!
<!-- #endregion -->

**Your Answers:**

1.
2.
3.
4.


## Summary

**What you learned:**

- **Python Philosophy**: Everything is an object with type and capabilities
- **Four Basic Types**: `int`, `float`, `str`, `bool`
- **Type Checking**: `type()` and `isinstance()`
- **Type Conversion**: Switching between types
- **Why Types Matter**: Safety, functionality, abstraction

