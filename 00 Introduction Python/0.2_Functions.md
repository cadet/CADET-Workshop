---
jupyter:
  jupytext:
    text_representation:
      extension: .md
      format_name: markdown
      format_version: '1.3'
      jupytext_version: 1.18.1
  kernelspec:
    display_name: base
    language: python
    name: python3
---

# 0.2 Functions - Creating Reusable Code Blocks

This notebook covers how to make our programs **reusable** and **organized**!

## The Philosophy: From Repetitive to Reusable

Imagine you're writing instructions:
- **Repetitive**: Write the same calculation 10 times for different numbers
- **Reusable**: Create a function once, use it everywhere with different inputs

**Functions** are like creating your own **custom methods** - just like strings have `.upper()` and lists have `.append()`!


# Functions - Reusable Code Blocks

In "0.1_Introduction_to_Types", we already used functions like `type()`, `int()`, and `str()` to work with different data types.

**Functions** are like creating your own **type methods**! 

## Why Functions Matter

Functions help us:
1. **Avoid repetition** - Write once, use many times
2. **Organize code** - Break complex problems into smaller parts  
3. **Make debugging easier** - Test parts individually
4. **Avoid mistakes** - Reuse tested code instead of rewriting

<!-- #region -->
## Defining Functions - The Basics

**Syntax**:
```python
def function_name(parameters):
    # do something
    return result  # optional
```

**Key Parts:**
- `def` - tells Python we're defining a function
- `function_name` - what we call our function
- `parameters` - input data (optional)
- `return` - output data (optional)

### **CADET-Process Connection:**
Functions are everywhere in CADET-Process! Examples:
- `process.add_unit_operation()` - adds columns, tanks, etc.
- `binding_model.calculate_parameters()` - computes adsorption parameters
- `simulator.simulate()` - runs the actual chromatography simulation
- `solution.plot()` - visualizes concentration profiles
<!-- #endregion -->

```python
# Basic function definitions - working with basic types

# 1. Simple function with no parameters
def greet():
    return "Hello, World!"

# 2. Function with parameters
def greet_person(name):
    return f"Hello, {name}!"

# 3. Function with multiple parameters
def greet_person_age(name, age):
    return f"Hello, {name}! You are {age} years old."

# 4. Function with default parameters
def greet_with_title(name, title="Friend"):
    return f"Hello, {title} {name}!"

# Using our functions
print("\nUsing our functions:")
print(f"  {greet()}")
print(f"  {greet_person('Anna')}")
print(f"  {greet_with_title('Clara')}")
print(f"  {greet_person_age('Bob', 25)}")
print(f"  {greet_with_title('David')}")
print(f"  {greet_with_title('Eve', title='Dr.')}")

#Q: What happens if we call greet_with_title with no "title" argument?
#print(f"  {greet_with_title('Eve', 'Dr.')}")
```

<!-- #region -->
## Function Scope - Variables Inside Functions
**Scope** determines where variables can be accessed in your code. Think of scope like rooms in a house - some things are private to each room, others are shared throughout the house.

### Types of Scope:

1. **Local Scope** - Variables defined inside a function
    - Only accessible within that function
    - Created when function is called, destroyed when function ends
    - Each function call gets its own local variables

2. **Global Scope** - Variables defined outside any function
    - Accessible everywhere in your program
    - Exist for the entire program lifetime
    - Can be read from inside functions, but need `global` keyword to modify


### Scope Rules:
- **Local variables** hide **global variables** with the same name
- Functions can **read** global variables automatically
- Functions need `global` keyword to **modify** global variables
- **Parameters** are always local to their function
Variables inside functions have their own **scope** (visibility area):
<!-- #endregion -->

```python
# Global variables (accessible everywhere) - basic types
global_message = "I'm global!"      # str type
global_counter = 0                  # int type

def demonstrate_scope(parameter):
    # Local variables (only accessible inside this function)
    local_message = "I'm local!"     # str type
    local_number = 42                # int type
    
    # We can read global variables
    print(f"  Inside function - Global message: {global_message}")
    print(f"  Inside function - Global counter: {global_counter}")
    print(f"  Inside function - Local message: {local_message}")
    print(f"  Inside function - Local number: {local_number}")
    print(f"  Inside function - Parameter: {parameter}")


# Test scope with different types
print(f"Before function calls - Global message: {global_message}")
print(f"Before function calls - Global counter: {global_counter}")

#Q: What happens if we try to access local_number here?
#print(f"  Inside function - Local number: {local_number}")

def modify_global():
    global global_counter  # Tell Python we want to modify the global
    global_counter += 1
    return f"Counter is now: {global_counter}"


print(f"\nModifying global counter:")
print(f"  {modify_global()}")
print(f"  {modify_global()}")
print(f"  {modify_global()}")
```

## Return Values and Multiple Returns

Functions can return different types of values to provide results back to the caller. Understanding return patterns helps you design functions that communicate effectively with the rest of your program.

### Types of Return Values:

1. **Single Return Value** - Most common pattern
    - Return one piece of data: number, string, boolean, etc.
    - Simple and straightforward to use

2. **Multiple Return Values** - Return tuple that can be unpacked
    - Python automatically packs multiple values into a tuple
    - Can be unpacked into separate variables or used as tuple

3. **Conditional Returns** - Different return types based on conditions
    - Return `None` for invalid input or errors
    - Return different data types based on function logic
    - Useful for error handling and validation

4. **No Return Value** - Functions that perform actions
    - Functions without `return` automatically return `None`
    - Used for functions that print, modify global state, or have side effects

### Return Value Best Practices:

- **Be Consistent**: Same function should return predictable types
- **Use None for Errors**: Return `None` or special values for invalid cases
- **Document Return Types**: Make it clear what your function returns
- **Type Checking**: Validate inputs to ensure reliable return values
- **Multiple Returns**: Use tuples for related data that belongs together

### Advanced Pattern - Functions Returning Functions:
Functions can even return other functions, creating powerful patterns for customization and specialization.

```python
# Return values and multiple returns
def simple_math(a, b):
    """Function returning a single value"""
    return a + b

def math_operations(a, b):
    """Function returning multiple values as a tuple"""
    addition = a + b
    subtraction = a - b
    multiplication = a * b

    return addition, subtraction, multiplication

def analyze_number(number):
    """Function returning different types based on analysis"""
    if not isinstance(number, (int, float)):
        return False, "Not a number"
    
    # Return: is_positive, description
    is_positive = number > 0
    description = f"{number} is {'positive' if is_positive else 'negative or zero'}"
    
    return is_positive, description

def safe_divide(a, b):
    """Function with different return types based on conditions"""
    if not isinstance(a, (int, float)) or not isinstance(b, (int, float)):
        return None  # Return None for invalid input
    
    if b == 0:
        return None  # Return None for error
    
    return a / b  # Return number for success

```

```python
# Test different return patterns
result = simple_math(10, 5)
print(f"   simple_math(10, 5) = {result} (type: {type(result).__name__})")
```

```python
# Multiple return values
add, sub, mul = math_operations(10, 3)
print(f"   Addition: {add}, Subtraction: {sub}, Multiplication: {mul}")
```

```python
# Can also capture all as tuple
all_results = math_operations(8, 2)
print(f"   All results as tuple: {all_results} (type: {type(all_results).__name__})")
```

```python
is_pos, desc = analyze_number(42)
print(f"   Number 42: positive={is_pos}, description='{desc}'")

# Test with invalid input
result_invalid = analyze_number("not a number")
print(f"   Invalid input: {result_invalid}")

test_cases = [(10, 2), (10, 0), ("hello", 5)]
for a, b in test_cases:
    result = safe_divide(a, b)
    print(f"   safe_divide({a}, {b}) = {result} (type: {type(result).__name__})")

# Functions returning functions (advanced)
def create_multiplier(factor):
    """Function that returns another function"""
    def multiplier(x):
        return x * factor
    return multiplier

# Create specialized functions
double = create_multiplier(2)
triple = create_multiplier(3)

print(f"   double(5) = {double(5)}")
print(f"   triple(4) = {triple(4)}")
print(f"   double function type: {type(double).__name__}")
```

# Exercises 



## Exercise 1: Type Analyzer

Create a function that analyzes values and provides detailed type statistics:
Write a function `analyze_value(value)` that:
- Takes a single value as input.
- Returns the type of the value as a string (e.g., "int", "str", "list").:


```python tags=["solution"]
# Exercise 1: Type Analyzer

def analyze_value(value):
    """
    Analyzes a value and returns detailed type information.
    
    Parameters:
    value: Any value to analyze
    
    Returns:
    str: The type name of the value
    """
    return type(value).__name__

# Test the function
test_values = [42, 3.14, "Hello", True, [1, 2, 3], {"key": "value"}]

print("Type Analysis:")
for value in test_values:
    result = analyze_value(value)
    print(f"Value: {value}, Type: {result}")

# CADET-Process relevant examples
concentration = 1.5  # mg/mL
component_name = "Protein_A"
is_converged = True
time_points = [0, 60, 120, 180]

print(f"\n CADET examples:")
print(f"Concentration {concentration}: {analyze_value(concentration)}")
print(f"Component name '{component_name}': {analyze_value(component_name)}")
print(f"Convergence flag {is_converged}: {analyze_value(is_converged)}")
print(f"Time points {time_points}: {analyze_value(time_points)}")
```

## Exercise 2: Number Classifier

Create a function that analyzes a list of numbers and categorizes them:
Write a function `analyze_number(num)` that:
- Takes a single number as input.
- Returns a tuple containing:
    - A boolean indicating if the number is positive.
    - The value of the number.

```python tags=["solution"]
# Exercise 2: Number Classifier

def analyze_number(num):
    """
    Analyzes a number and categorizes it.
    
    Parameters:
    num: Value to analyze
    
    Returns:
    tuple: (is_positive: bool, value: int)
    """

    positive = num > 0
    value = num

    return positive, value

# CADET-Process examples
flow_rate = 1.5        # mL/min (positive)
temperature_change = -5  # °C (negative)
initial_concentration = 0  # mg/mL (zero)

print(f"Flow rate {flow_rate}: {analyze_number(flow_rate)}")
print(f"Temperature change {temperature_change}: {analyze_number(temperature_change)}")
print(f"Initial concentration {initial_concentration}: {analyze_number(initial_concentration)}")
```

## Exercise 3: Mathematical Calculator

Create a calculator system that works with different numeric types:

Write a function that 
- Adds two numbers
- Subtracts two numbers
- Multiplies two numbers


```python
# Exercise 3: Mathematical Calculator

def add_numbers(a, b):
    """
    Adds two numbers.
    
    Parameters:
    a: First number
    b: Second number
    
    Returns:
    Sum of a and b
    """
    return a + b

def subtract_numbers(a, b):
    """
    Subtracts two numbers.
    
    Parameters:
    a: First number
    b: Second number
    
    Returns:
    Difference of a and b (a - b)
    """
    return a - b

def multiply_numbers(a, b):
    """
    Multiplies two numbers.
    
    Parameters:
    a: First number
    b: Second number
    
    Returns:
    Product of a and b
    """
    return a * b

# Test the calculator functions
print("Mathematical Calculator:")

# Test addition
result1 = add_numbers(10, 5)
print(f"10 + 5 = {result1}")

result2 = add_numbers(3.14, 2.86)
print(f"3.14 + 2.86 = {result2}")

# Test subtraction
result3 = subtract_numbers(20, 8)
print(f"20 - 8 = {result3}")

result4 = subtract_numbers(15.5, 3.2)
print(f"15.5 - 3.2 = {result4}")

# Test multiplication
result5 = multiply_numbers(6, 7)
print(f"6 * 7 = {result5}")

result6 = multiply_numbers(2.5, 4.0)
print(f"2.5 * 4.0 = {result6}")

# CADET-Process examples
print("\nCADET-Process calculations:")

# Column volume calculation: π * r² * h
radius = 0.5  # cm
height = 10.0  # cm
pi_approx = 3.14159

# Calculate area: π * r²
radius_squared = multiply_numbers(radius, radius)
area = multiply_numbers(pi_approx, radius_squared)
volume = multiply_numbers(area, height)
print(f"Column volume: π * {radius}² * {height} = {volume:.2f} cm³")

# Flow rate calculation: sum of two pumps
pump_a_flow = 1.2  # mL/min
pump_b_flow = 0.8  # mL/min
total_flow = add_numbers(pump_a_flow, pump_b_flow)
print(f"Total flow rate: {pump_a_flow} + {pump_b_flow} = {total_flow} mL/min")

# Concentration difference
initial_conc = 10.5  # mg/mL
final_conc = 7.3     # mg/mL
conc_decrease = subtract_numbers(initial_conc, final_conc)
print(f"Concentration decrease: {initial_conc} - {final_conc} = {conc_decrease} mg/mL")

# Buffer preparation: dilution factor
stock_conc = 1000    # mM
target_conc = 50     # mM
dilution_factor = multiply_numbers(stock_conc / target_conc, 1)
print(f"Dilution factor: {stock_conc}/{target_conc} = {dilution_factor}x")
```

## Reflection

Answer these questions:

1. **How do conditional statements help make programs more intelligent?**
2. **What's the difference between `for` loops and `while` loops? When would you use each?**
3. **How are functions similar to the methods we learned about for lists and strings?**
4. **What makes a function "reusable" and why is that important?**
5. **How do all the concepts (types, containers, control flow, functions) work together?**

**Write your answers:**


**My Answers:**

1. 
2. 
3. 
4. 
5. 


## Summary

**What you learned:**

### Functions
- ✅ **Function Definition**: Creating reusable code blocks with `def`
- ✅ **Parameters and Arguments**: Passing data to functions (positional, keyword, default)
- ✅ **Return Values**: Getting results from functions (single, multiple, different types)
- ✅ **Variable Scope**: Understanding local vs global variables
- ✅ **Type Safety**: Using type checking and validation in functions

### Integration with Basic Types
- **int**: Used in calculations, counters, and mathematical operations
- **float**: Used in mathematical computations and scientific calculations
- **str**: Used for text processing, formatting, and user interaction
- **bool**: Used for validation, flags, and conditional returns
- **Type checking**: `isinstance()` and `type()` for robust function design
- **Type conversion**: `int()`, `float()`, `str()`, `bool()` within functions

### Key Function Patterns 
- **Pure Functions**: Same input always gives same output (predictable)
- **Side Effects**: Functions that modify global state or print output
- **Factory Functions**: Functions that create and return other functions
- **Validation Functions**: Functions that check and validate input types
- **Conversion Functions**: Functions that transform data between types
- **Utility Functions**: Small, reusable helper functions

**Key Insights:**
- **Functions** make code **reusable** and **organized**
- **Type checking** is crucial for **robust** function design
- **Basic types** are the foundation for all function logic
- **Lambda functions** are perfect for simple, immediate operations
- **Multiple returns** allow functions to provide rich information
- **Good function design** prevents errors and makes code maintainable

**Philosophy Reinforced:**
- Everything is an object with **type** and **capabilities**
- Functions are like **custom methods** we create for our specific needs
- **Type safety** prevents errors and makes code more reliable
- **Reusability** is key to efficient programming

---
*"Functions organize our thinking into reusable blocks. Like having a toolbox where each tool has a specific, reliable purpose!"*
